// sample example — one-shot buffer playback
// Uses the built-in kick sample loaded at boot.
// Ctrl+Enter to evaluate, Ctrl+. to stop.

set(tempo(120))

sample drums [\kick \kick _ \kick]'maybe(0.85)
